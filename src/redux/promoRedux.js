/* selectors */

export const getPromoData = ({ promo }) => promo;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
